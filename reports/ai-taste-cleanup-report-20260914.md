# 全站「去 AI 味」文风监测与整改报告（2026-09-14）

> 主理人依据「使命愿景价值观 + 质量目标」对全站做内容质量深度巡检的收尾项。
> 用户原问：**「每一篇文章是否都经过去 AI 味监测？」**

## 一、诚实回答：分两层

| 层 | 覆盖范围 | 状态 |
|---|---|---|
| **机械层**（调性违禁词 / 结构信号 / 设计品味） | **全站每篇都覆盖** | quality_gate 22 门含 Gate5 品味关，每篇发布必过；独立 `taste_gate.py --all` 亦对全站生效（290 篇） |
| **审美层**（破折号 0 / 半角 0 / 对称腔 / 报幕句 / 匠气 coda / 物理借词 / 诗意隐喻） | **此前未全站机器兜底** | 本次新建 `monitor_ai_taste.py` 全站扫描，发现系统性缺口并整改 |

**结论**：机械层已全覆盖；审美层此前是盲区（Gate5 只查设计品味，不查破折号/半角），本次补上。

## 二、全站扫描结果（290 篇已索引文章，原始报告见 ai-taste-monitor-20260914.md）

| 信号 | 命中 | 真实违规判定 |
|---|---|---|
| 破折号违规（正文 `—`/`--`） | 179 篇 / 1467 处 | **系统性真违规**，违反发布铁律「破折号 0」 |
| 半角标点（中文邻半角） | 21 篇 / 195 处 | 基本真违规（不含数字/英文密集误报） |
| 对称腔 / 物理借词 / 报幕句 | 200+ 篇 | **高误报**，正常用法（"从…到""赋能"）被误标，不发起机械整改 |
| 营销水文 / 诗意隐喻 / 翻译腔 | 个位数 | 经核验多为误报，非缺陷 |

## 三、本次整改（主理人拍板，已原子发布）

**范围**：高曝光 / 高信号优先的 **28 篇**（破折号密度 Top + 半角违规篇），其余 151 篇破折号违规列为 backlog 随后续发布自然收敛（不批量机械改，避免误伤）。

**处置**：
- 正文级破折号 `—`/`–`/`--`/`——` → 逗号 `，`；年份跨度（em/en dash 数字间）→ `至`。
- 中文邻半角标点 → 全角（`：`/`，`/`；` 等）。
- 保护 `<script>/<style>/<pre>/<code>` 不被误改（prompt 模板 / JSON-LD / 代码块保留半角）。
- 2 篇工具/手册类（`hr-ai-prompt-toolbox-21`、`hr-bigfive-recruitment-screening`）的半角在 prompt 模板里合法，**回滚保留**。

**质量门**：28 篇 quality_gate 22/22 全绿；稳定性自检通过；sitemap 已重建。

## 四、执行纪律复盘（重点）

1. **信任但验证**：初版清理脚本的 YEAR 正则**误含 ASCII `-`**，把 `<time datetime="2026-01-10">`、URL `2026-08-14-m365` 等日期/链接错改成 `2026至01至10`。复验发现该回归后，立即将 28 篇**从线上真实 main 还原干净态**，再跑修正版脚本（YEAR 仅匹配 em/en dash），彻底消除损坏。日期/URL 经核验 **0 损坏**。
2. **双破折号→双逗号瑕疵**：原文 `——`（双 U+2014）被替换成 `，，`，已加「中文标点去重」步骤消除（复验 0 残留）。
3. **不盲信计数**：对称腔 242 篇、半角部分命中经带语境核验确认为误报（正常用法 + 数字密集误报），未发起破坏性批量整改。

## 五、仍待办（backlog，非阻塞）

- **B7**：全站其余 151 篇破折号违规随后续文章发布/修订自然收敛（发布铁律已含「破折号 0 / 半角 0」，新文不再产生；存量旧文按优先级逐步清）。
- **B8**：建议把"中文邻半角""正文破折号"两项**固化进 quality_gate 发布双闸**（Gate5 扩展），让"破折号 0 / 半角 0"从纪律变成机器兜底，杜绝本次盲区再现。

## 六、交付物

- 扫描器：`scripts/monitor_ai_taste.py`（可复用，全站 290 篇）
- 清理器：`scripts/_clean_aitaste_final.py`（修正版，已验证安全）
- 原始信号：`reports/ai-taste-monitor-20260914.md` / `.json`
- 本报告：`reports/ai-taste-cleanup-report-20260914.md`
