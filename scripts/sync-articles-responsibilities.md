#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
sync-articles.py 职责清单

本文档定义 sync-articles.py 脚本的完整职责范围，
每个输出块必须有且仅有一个明确写作者。
对应 LSN-20260819-001：同步脚本职责盲区禁存。

生成时间：2026-08-19
更新历史：
  - 2026-08-19: 初始版本，基于 INC-20260819-001 复盘决议

================================================================================
输出块清单
================================================================================

1. assets/js/article-index.json
   - 写作者: rebuild_index_json()
   - 触发: main() 第 549 行调用
   - 输入: articles 列表（已排除 redirect stub）
   - 校验: Gate 16 文章索引完整性关

2. articles/index.html 的 article-grid 区域
   - 写作者: rebuild_index_html()
   - 触发: main() 第 553 行调用
   - 输入: articles 列表（已排序）
   - 校验: Gate 17 内链图谱健康关（卡片数量）

3. articles/index.html 的 JSON-LD ItemList（CollectionPage）
   - 写作者: rebuild_index_jsonld()（新增，2026-08-19）
   - 触发: main() 第 557 行调用
   - 输入: articles 列表（已排序）
   - 校验: Gate 19 结构化数据合法性关

4. 分类页（categories/*.html）
   - 写作者: gen_categories()
   - 触发: main() 第 560 行调用
   - 输入: articles 列表 + tags_map
   - 校验: 无专门门禁，依赖整体一致性

5. 标签页（tags/*.html）
   - 写作者: gen_tags()
   - 触发: main() 第 561 行调用
   - 输入: articles 列表 + tags_map
   - 校验: 无专门门禁，依赖整体一致性

6. 标签云页（tags/index.html）
   - 写作者: gen_tag_index()
   - 触发: main() 第 562 行调用
   - 输入: articles 列表 + tags_map
   - 校验: 无专门门禁，依赖整体一致性

================================================================================
同步纪律
================================================================================

规则 1：每次运行前，先读取现有文件，增量更新，禁全量重写
规则 2：articles 列表必须经过 extract_article_info() 过滤（排除 stub）
规则 3：所有写函数使用同一份 articles 列表（已排序），禁止各自重排
规则 4：JSON-LD ItemList 必须与 article-grid 同步重建，禁止异步更新
规则 5：新增输出块必须在 main() 中显式调用，禁止静默缺失

================================================================================
验证机制
================================================================================

每次运行后必须执行：
  1. quality_gate.py --all（22 关全过）
  2. stability_guard.py --all（11 关全过）
  3. Gate 11 check_6_duplicates_and_anomalies（多源查重）

================================================================================
历史变更记录
================================================================================

2026-08-19（INC-20260819-001 修复）:
  - 新增 rebuild_index_jsonld() 函数
  - 在 main() 中调用，同步重建 JSON-LD ItemList
  - 根因：旧版只重建 article-grid，JSON-LD 长期 stale

================================================================================
EOF
